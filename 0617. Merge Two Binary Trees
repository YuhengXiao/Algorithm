/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    public TreeNode mergeTrees(TreeNode root1, TreeNode root2) {
        return dfs(root1, root2);
    }
    public TreeNode dfs(TreeNode root1, TreeNode root2){
        if(root1 == null & root2 != null){
            return root2;
        } else if(root1 != null & root2 == null){
            return root1;
        } else if(root1 == null & root2 == null){
            return null;
        }
        TreeNode ans = new TreeNode();
        ans.val = root1.val + root2.val;
        ans.left = dfs(root1.left, root2.left);
        ans.right = dfs(root1.right, root2.right);
        return ans;
    }
}

/*
Notes:

(1.	“Void-type Recursion (no return value)” is more suitable for modifying values of the original object (return object itself).
2.	“Return-type Recursion (return value)” is more suitable for passing values of the original object into a new assigned object (return new assigned ed object).)

1.	“Return-type Recursion”: 
(1)	A base case, where you look at the list and can just say the answer. This might be when the list is empty (len(l) == 0), but there could be others.
        if(root1 == null & root2 != null){
            return root2;
        } else if(root1 != null & root2 == null){
            return root1;
        } else if(root1 == null & root2 == null){
            return null;
        }
(2)	Something that processes the first element:
        TreeNode ans = new TreeNode();
        ans.val = root1.val + root2.val;
(3)	Something that processes the rest elements:
        ans.left = dfs(root1.left, root2.left);
        ans.right = dfs(root1.right, root2.right);
(4)	Something that combines the results of steps (2) and (3) and return the combination.
        return ans;

*/
