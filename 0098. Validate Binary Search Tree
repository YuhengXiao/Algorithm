/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    public boolean isValidBST(TreeNode root) {
        int result = -1;
        iterator(root, result);
        if (result == -1){
            return true;
        }
        return false;
    }

    public void iterator(TreeNode root, int result){
        if (root == null){
            return;
        }
        if (check(root) == false){
            result = 1;
        }
        iterator(root.left, result);
        iterator(root.right, result);
    }

    public boolean check(TreeNode root){
        if (root.left != null && root.right != null){
            if (root.val >= root.left.val && root.val <= root.right.val){
                 return true;
            }
        }
        else if (root.left != null && root.right == null){
            if (root.val >= root.left.val){
                return true;
            }
        }
        else if (root.left == null && root.right != null){
            if (root.val <= root.right.val){
                return true;
            }
        }
        else if (root.left == null && root.right == null){
            return true;
        }
        return false;
    }
}

/*
Notes:

(In binary tree, using “In-order traversal” we can get an ascending order. With this property, we can tell if a tree is a valid binary tree by using a stack.)

1.	Left node: In the binary tree, each left node is less than its parent.
2.	Right node: In the binary tree, each right node is larger than its parent.
3.	Parent node: In the binary tree, parent node is larger than any nodes of its left branch, is smaller than any nodes of its right branch.

*/
