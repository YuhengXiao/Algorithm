public class Solution {
    // you need to treat n as an unsigned value
    public int hammingWeight(int n) {
        int count = 0;
        for (int i = 0; i < 32; i++){
            if ((1 & n) == 1){
                count++;
            }
            n = n >> 1;
        }
        return count;
    }
}

/*
Notes:

1.	“1 & n”: to get the last digit of the “bit”.
(1)	If the last digit is 1, return 1 (because 1 & 1 = 1);
(2)	If the last digit is 0, return 0 (because 1 & 0 = 0);
            if ((1 & n) == 1){count++;}

*/
