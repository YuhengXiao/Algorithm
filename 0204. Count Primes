class Solution {
    public int countPrimes(int n) {
        int ans = 0;
        int[] cashe = new int[n + 2];
        for (int i = 2; i < n; i++){
            if (cashe[i] == 0){
                ans++;
                for (int j = 1; j * i < n; j++){
                    cashe[j * i] = 1;
                }
            }
        }
        return ans;
    }
}

/*
Notes:

(Using “Sieve of Eratosthenes” to find “Prime numbers”.)

1.	“Sieve of Eratosthenes”: 
  (1)	Creating an array: The current index is a “prime number” if the current element’s value is 0, or is a “composite number” if the current element’s value is 1.
  (2)	Finding all possible “Composite numbers”: If the current index is a “prime number”, we’re marking all “multiples of 2” to “Composite numbers” (by setting their values to 1).


https://www.youtube.com/watch?v=5LMkddl2NCk&t=179s

*/
