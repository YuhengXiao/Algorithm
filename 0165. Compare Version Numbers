class Solution {
    public int compareVersion(String version1, String version2) {
        String[] arr1 = version1.split("\\.");
        String[] arr2 = version2.split("\\.");
        int minlen = Math.min(arr1.length, arr2.length);
        int maxlen = Math.max(arr1.length, arr2.length);
        for (int i = 0; i < minlen; i++){
            if (Integer.valueOf(arr1[i]) < Integer.valueOf(arr2[i])){
                return -1;
            }
            if (Integer.valueOf(arr1[i]) > Integer.valueOf(arr2[i])){
                return 1;
            }
        }
        for (int i = minlen; i < maxlen; i++){
            if(arr1.length < arr2.length){
                if (Integer.valueOf(arr2[i]) != 0){
                    return -1;
                }
            }
            if(arr1.length > arr2.length){
                if (Integer.valueOf(arr1[i]) != 0){
                    return 1;
                }
            }
        }
        return 0;
    }
}

/*
Notes:

(“Using split() + regular expression”.)

1.	“.split()”: method breaks a given string around matches of the given regular expression and returns a string array.
2.	String[] arr1 = version1.split("\\.");

•	In regular expression: “.” Stands for “Find just one instance of any character”, so we need “\\.” to redefine “.”, then it stands for just the character “.”.

*/
