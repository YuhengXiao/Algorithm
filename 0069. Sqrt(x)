class Solution {
    public int mySqrt(int x) {
        int l = 0, r = x;
        int ans = -1;
        while (l <= r){
            int mid = (l + r) / 2;
            if ((long)mid * mid == x){
                return mid;
            }
            if ((long)mid * mid > x){
                r = mid - 1;
            }
            if ((long)mid * mid < x){
                ans = mid;
                l = mid + 1;
            }
        }
        return ans;
    }
}

/*
Notes:

(Using “Binary Search” to find the “square root(decimal digits are truncated)” of a number)

1.	“Converting the type of Integer”: Using “round bracket”.

--------------double => int-----------------
        double data = 3452.345;  
        // convert into int
        int value = (int)data;
--------------------------------------------
*/
