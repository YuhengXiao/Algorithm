class Solution {
    public int smallestFactorization(int num) {
        if (num < 2){
            return num;
        }
        String ans = "";
        for (int i = 9; i >= 2; i--){
            while(num % i == 0){
                ans = String.valueOf(i) + ans;
                num = num / i;
            }
        }
        if (num != 1){
            return 0;
        }
        return Long.valueOf(ans) <= Integer.MAX_VALUE ? Integer.valueOf(ans) : 0;
    }
}

/*
Notes:

(Using “Greedy Algorithm” to find out the smallest number satisfies conditions by trying to put the biggest number (from 9 to 2) from last digit to first digit.)
*/
