/**
 * Definition for singly-linked list.
 * class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode(int x) {
 *         val = x;
 *         next = null;
 *     }
 * }
 */
public class Solution {
    public boolean hasCycle(ListNode head) {
        HashSet<ListNode> count = new HashSet<ListNode>();
        while(head != null){
            if (count.contains(head)){
                return true;
            }
            count.add(head);
            head = head.next;
        }
        return false;
    }
}

/
Notes:

(Using HashSet to tell whether or not a Linked List is a Cycle Linked List (checking if there are duplicates ListNodes).)
/
